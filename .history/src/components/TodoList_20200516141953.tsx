import React from 'react';

const TodoList:React.FC = () => {
    const todos = [{id: 't1', text:'Finish the course!'}]
    return (
        <ul>
            
        </ul>
    );
};

export default TodoList:React.FC;